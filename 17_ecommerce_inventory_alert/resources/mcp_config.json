{
  "mcpServers": {
    "project": {
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/mcp-server"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}